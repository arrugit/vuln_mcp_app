# Backend / control-plane dependencies (TDD §4 stack).
# Pinned to compatible minimum versions; ordinarily-secure infrastructure.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
sqlmodel>=0.0.19
SQLAlchemy>=2.0,<3.0
httpx>=0.27,<1.0

# Official Python MCP SDK (D-02). The MCP server/client wire transport
# (streamable HTTP, D-03) is wired in a later phase; Phase 0 uses an in-process
# registry transport so the control plane is testable without a container.
mcp>=1.2.0

# Testing (TDD §29). httpx above doubles as the FastAPI TestClient transport.
pytest>=8.2,<9.0
