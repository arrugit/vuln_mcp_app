"""Lab fixtures package.

Each ``labs/<vuln>/`` sub-package holds the synthetic data + write-up for one
lab (TDD §5 repository structure). Fixtures are loaded by the MCP tool handlers
and the DB seed. All data here is synthetic (SEC-002).
"""
