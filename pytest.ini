[pytest]
# TDD §29 testing architecture. Tests live under tests/ and import the backend
# package + the physically-separate mcp_servers package from the repo root.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
